# 轻屿论坛公网部署说明

目标：得到一个 `https://...` 链接，发到微信里，别人手机点开就能登录、发帖、回帖。

## 推荐路线

### 路线 A：最快体验公网链接

适合先发给少量朋友测试。

1. 注册 Render、Railway、Fly.io 等支持 Python Web 服务的平台。
2. 把 `outputs` 文件夹里的文件上传到 GitHub 仓库。
3. 在平台中新建 Web Service。
4. 启动命令填：

```bash
python -u server.py
```

5. 平台会给你一个 `https://...` 链接，把这个链接发到微信即可打开。

注意：免费或海外平台在国内访问可能慢；免费实例可能休眠；本项目现在用文件保存数据，部分平台重启后数据可能丢失。

建议同时设置环境变量：

```bash
ADMIN_KEY=你自己的管理员口令
```

### 路线 B：更适合国内微信用户

适合后续认真内测。

1. 买腾讯云轻量应用服务器或云服务器。
2. 把 `outputs` 文件夹上传到服务器。
3. 在服务器上运行：

```bash
python3 -u server.py
```

4. 先用 `http://服务器公网IP:4173` 测试。
5. 购买域名并解析到服务器。
6. 配置 HTTPS 证书。
7. 如果服务器在中国大陆，按要求完成 ICP 备案。
8. 最终把 `https://你的域名` 发到微信。

## 当前版本能力

- 昵称登录。
- 多人看到同一批帖子。
- 发帖、回帖、点赞、举报。
- 下拉刷新。
- 删除自己的帖子和回复。
- 点击头像/昵称查看别人主页。
- 编辑 MBTI、学历、城市、行业等个人标签。
- 学历认证申请和管理员审核。
- 管理员权限区分，默认口令是 `qingyu-admin-2026`，正式部署请修改 `ADMIN_KEY`。

## 正式上线前建议补强

- 手机号、微信或邮箱验证码登录。
- 数据库替代本地 `data/db.json`。
- 管理员权限，不让普通用户进入管理中心。
- HTTPS、域名、备案、内容安全审核。
- 定期备份数据。
